# node_modules → ローカル依存なので毎回再構築される
node_modules

# .next → Next.jsのビルド成果物（本番では不要）
.next

# OS関連（macの場合）
.DS_Store

# ログファイル
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 環境変数ファイル（今回は使ってないが将来のために）
.env
.env.local
.env.production
.env.development

# Vercel用（ローカルのvercel CLI用ファイル）
.vercel

# vscodeなどエディタの設定（共有しないなら）
.vscode
